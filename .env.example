# Email Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-app-password-here
RECIPIENT_EMAILS=recipient1@email.com,recipient2@email.com

# Web Hosting Configuration
# Set this to the public URL where the weekly report is hosted.
# Use your Netlify URL, GitHub Pages URL, S3 endpoint, or custom domain.
# Examples:
#   https://your-site.netlify.app            (Netlify)
#   https://yourusername.github.io/repo-name  (GitHub Pages)
#   https://your-bucket.s3.amazonaws.com     (S3)
#   https://reports.climatecardinals.org     (custom domain)
# The automated newsletter email uses this value when it inserts a "View All Data" link.
# Update the GitHub secret named WEB_REPORT_BASE_URL to match this URL so the weekly
# workflow sends the correct link.
WEB_REPORT_BASE_URL=

# SMTP Configuration (optional, defaults to Gmail)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
